version: '3.8'
services:
  frontend:
    build: .
    ports:
      - "3000:80" # Maps internal port 80 to server port 3000
    restart: unless-stopped

  backend:
    build: ./src/backend
    ports:
      - "5000:5000" # Maps internal port 5000 to server port 5000
    restart: unless-stopped
